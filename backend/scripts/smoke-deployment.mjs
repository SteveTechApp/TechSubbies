const base=(process.env.SMOKE_API_URL||"http://localhost:4000/api").replace(/\/$/,"");for(const endpoint of["health","ready"]){const response=await fetch(`${base}/${endpoint}`,{signal:AbortSignal.timeout(5000)});if(!response.ok)throw new Error(`${endpoint} failed with HTTP ${response.status}`);const body=await response.json();console.log(JSON.stringify({endpoint,status:response.status,body}));}
