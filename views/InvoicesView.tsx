import React from "react";
import { useAppContext } from "../context/InteractionContext";
import { FileText } from "../components/Icons";

export const InvoicesView=()=>{const{invoices}=useAppContext();const membershipInvoices:any[]=invoices as any[];return <div><h1 className="mb-2 flex items-center text-3xl font-bold"><FileText size={30} className="mr-3 text-blue-600"/>Membership invoices</h1><p className="mb-6 text-sm text-gray-600">These invoices relate only to TechSubbies membership. Engineers and clients invoice and pay each other directly for assignments.</p><div className="overflow-hidden rounded-lg bg-white shadow">{membershipInvoices.length===0?<p className="p-8 text-center text-gray-500">No membership invoices issued yet.</p>:<table className="min-w-full divide-y divide-gray-200"><thead className="bg-gray-50"><tr><th className="px-6 py-3 text-left text-xs uppercase text-gray-500">Invoice</th><th className="px-6 py-3 text-left text-xs uppercase text-gray-500">Membership plan</th><th className="px-6 py-3 text-left text-xs uppercase text-gray-500">Amount</th><th className="px-6 py-3 text-left text-xs uppercase text-gray-500">Issued</th><th className="px-6 py-3 text-left text-xs uppercase text-gray-500">Status</th></tr></thead><tbody className="divide-y divide-gray-200">{membershipInvoices.map(invoice=><tr key={invoice.id}><td className="px-6 py-4 text-sm font-medium">{invoice.id}</td><td className="px-6 py-4 text-sm capitalize">{invoice.plan||"membership"}</td><td className="px-6 py-4 text-sm font-bold">{new Intl.NumberFormat("en-GB",{style:"currency",currency:invoice.currency||"GBP"}).format((invoice.amountPence||0)/100)}</td><td className="px-6 py-4 text-sm text-gray-500">{new Date(invoice.createdAt).toLocaleDateString("en-GB")}</td><td className="px-6 py-4 text-sm capitalize">{invoice.status}</td></tr>)}</tbody></table>}</div></div>};
