import React,{useState} from "react";
import apiService from "../services/apiService";

export default function AccountRecoveryPage(){const path=window.location.pathname;const token=new URLSearchParams(window.location.search).get("token")||"";const verify=path==="/verify-email";const reset=path==="/reset-password";const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);
 async function submit(event:React.FormEvent){event.preventDefault();setBusy(true);setError("");try{if(verify){await apiService.confirmEmail(token);setMessage("Your email has been verified. You can now sign in.");}else if(reset){await apiService.resetPassword(token,password);setMessage("Your password has been changed and previous sessions have been signed out.");}else{await apiService.requestPasswordReset(email);setMessage("If an account exists, password reset instructions have been sent.");}}catch(value:any){setError(value.message||"The request could not be completed.");}finally{setBusy(false);}}
 return <main className="min-h-screen bg-slate-950 px-5 py-12 text-white"><form onSubmit={submit} className="mx-auto max-w-md rounded-3xl border border-white/10 bg-slate-900 p-7"><p className="text-xs font-bold uppercase tracking-[.25em] text-cyan-300">Secure account access</p><h1 className="mt-3 text-3xl font-bold">{verify?'Verify email':reset?'Choose a new password':'Reset password'}</h1>{!token&&(verify||reset)?<p className="mt-5 rounded-xl bg-red-950 p-4 text-red-100">This link is incomplete. Request a new account email.</p>:<>{!verify&&!reset&&<label className="mt-6 block text-sm">Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 px-4 py-3"/></label>}{reset&&<label className="mt-6 block text-sm">New password<input type="password" minLength={12} required value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 px-4 py-3"/><span className="mt-2 block text-xs text-slate-400">Use at least 12 characters.</span></label>}{error&&<p className="mt-4 rounded-xl bg-red-950 p-3 text-sm text-red-100">{error}</p>}{message&&<p className="mt-4 rounded-xl bg-emerald-950 p-3 text-sm text-emerald-100">{message}</p>}{!message&&<button disabled={busy} className="mt-6 w-full rounded-xl bg-cyan-300 px-5 py-3 font-bold text-slate-950 disabled:opacity-60">{busy?'Working…':verify?'Verify email':reset?'Change password':'Send reset link'}</button>}</>}<a href="/login" className="mt-6 block text-center text-sm font-bold text-cyan-300">Return to sign in</a></form></main>}
