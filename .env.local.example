# Copy this file to .env.local to configure the frontend.

# Where the backend API is running. Local development defaults to
# http://localhost:4000/api. Production builds require this value and
# require HTTPS, unless using a same-origin root-relative path such as /api.
VITE_API_BASE_URL="http://localhost:4000/api"
