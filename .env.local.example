# Copy this file to .env.local to configure the frontend.

# Where the backend API is running. Defaults to http://localhost:4000/api
# if not set, which matches the backend's default port (see backend/.env.example).
VITE_API_BASE_URL="http://localhost:4000/api"
